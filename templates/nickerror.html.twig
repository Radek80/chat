{% extends 'base.html.twig' %}
{% block title %} Błąd {% endblock %}
{% block form %}
    <h2>Proszę podać nick!</h2>

    <form method="POST" action="{{ path('login') }}">
        <button class="btn btn-dark" type="submit">Powrót</button>
    </form>
{% endblock %}
